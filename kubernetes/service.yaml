# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: pacman
  namespace: vunguye1
spec:
  type: ClusterIP
  selector:
    app: pacman
  ports:
    - name: web
      port: 80
      targetPort: http